<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kantahliliuygulamasi">

    <!-- Gerekli izinler -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Kamera özelliği isteğe bağlı -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.KanTahliliUygulamasi"
        android:requestLegacyExternalStorage="true">

        <!-- Google Maps API anahtarı -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD7evOCakMXRbogfFMdaxU0fCKQTJTxLPI" />

        <!-- Aktiviteler -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".RegisterActivity" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".OcrActivity" android:exported="true" />
        <activity android:name=".Ocr1Activity" android:exported="true" />
        <activity android:name=".YeniActivity" android:exported="true" />

        <!-- Test ile ilgili aktiviteler -->
        <activity android:name=".GecmisTestlerActivity" android:exported="true" />
        <activity android:name=".TestDetayActivity" android:exported="true" />

        <activity android:name=".ReminderActivity" android:exported="true" />
        <activity android:name=".BMIActivity" android:exported="true" />
        <activity android:name=".MapsActivity" android:exported="true" />
        <activity android:name=".SonucActivity" android:exported="true" />

        <!-- Yeni eklenen OrnekActivity -->
        <activity android:name=".OrnekActivity" android:exported="true" />

        <!-- Alarm Receiver -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SCHEDULE_EXACT_ALARM">
            <intent-filter>
                <action android:name="com.example.kantahliliuygulamasi.ACTION_SET_REMINDER" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
